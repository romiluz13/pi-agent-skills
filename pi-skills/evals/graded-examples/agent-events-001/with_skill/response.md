For **`prompt()`**, **`pi-mono/packages/agent/README.md`** documents the **### prompt() Event Sequence** tree: **`agent_start` → `turn_start` → user `message_start`/`message_end` → assistant `message_start` → streaming **`message_update`** chunks → assistant `message_end` → **`turn_end`** → **`agent_end`** (with an expanded branch when tools run: **`tool_execution_*`**, tool result messages, extra turns).
