`pi-mono/packages/coding-agent/src/core/resource-loader.ts` **`reload()`** calls **`detectExtensionConflicts`** on the loaded extensions, then **pushes each conflict into `extensionsResult.errors`** alongside other load errors. The inline comment states conflicts are **diagnostics** and **precedence follows load order** — **extensions are not dropped**; all stay loaded while users see the errors.
